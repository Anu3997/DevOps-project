apiVersion: v1
kind: Service
metadata:
  name: devops-service

spec:
  type: NodePort
  selector:
    app: devops-app
    color: green

  
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30007